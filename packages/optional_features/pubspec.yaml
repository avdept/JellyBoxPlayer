name: optional_features
description: >
  JellyBox features the app builds without: UPnP renderer quirks, genre-mix
  playlist policy, and cross-device continuity. This is the open stub; the
  working implementation is a private package swapped in through
  pubspec_overrides.yaml.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.27.0
  very_good_analysis: ^10.1.0
